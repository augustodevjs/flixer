name: flixer-catalog-ci-workflow

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      UNIT_TESTS_FLIXER: ../tests/Flixer.Catalog.UnitTest/Flixer.Catalog.UnitTest.csproj
      INTEGRATION_TESTS_FLIXER: ../tests/Flixer.Catalog.IntegrationTests/Flixer.Catalog.IntegrationTests.csproj
      ENDTOEND_TESTS_FLIXER: ../tests/Flixer.Catalog.EndToEndTests/Flixer.Catalog.EndToEndTests.csproj

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "6.x"

      - name: Restore Dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore

      - name: Run Unit Tests
        run: dotnet test --no-build --verbosity normal ${{ env.UNIT_TESTS_FLIXER }}
        working-directory: tests/Flixer.Catalog.UnitTest

      - name: Run Integration Tests
        run: dotnet test --no-build --verbosity normal ${{ env.INTEGRATION_TESTS_FLIXER }}
        working-directory: tests/Flixer.Catalog.IntegrationTests

      - name: Run End-to-End Tests
        run: dotnet test --no-build --verbosity normal ${{ env.ENDTOEND_TESTS_FLIXER }}
        working-directory: tests/Flixer.Catalog.EndToEndTests
