version: '3.4'

services:
    flixer.catalog.db:
        container_name: catalogdb-e2e
        hostname: catalogdb-e2e
        image: mysql:latest
        ports: 
            - 3002:3306
        environment:
            MYSQL_ROOT_PASSWORD: Flixer
